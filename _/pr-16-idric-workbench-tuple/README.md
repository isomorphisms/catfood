# PR #16 archive — Idriç workbench tuple

This directory preserves the complete changed-file snapshot, intent, and historical context of Cat Food PR #16, **“Build and verify the Idriç workbench tuple.”**

It is reference material, not active Cat Food implementation. The original branch had become non-mergeable and Cat Food's architecture changed substantially afterward. The code is kept here so later work can recover a requirement or design decision without keeping the stale pull request open.

- `PURPOSE.md` records what the work was trying to accomplish.
- `CONTEXT.md` records the surrounding architecture and the failure that motivated part of the design.
- `STATUS-2026-09-23.md` records what current Cat Food already does and what remains worth reconsidering.
- `ORIGINAL-PR.md` preserves the pull-request description and metadata.
- `COMMITS.md` records the historical commits.
- `code/` contains every file changed by the PR, copied from exact head `43922a89bca81d80b13fbe7b4043330000409edf` with its original repository path.

Nothing under this directory should be treated as automatically current or copied back into production without comparison against current `main`.
